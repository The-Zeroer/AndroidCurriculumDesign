<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".UserApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurriculumDesign">
        <activity
            android:name=".feature.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.main.MainActivity"
            android:exported="true"/>
        <activity
            android:name=".feature.chat.ChatActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".feature.about.AboutActivity"
            android:exported="true"/>

        <!-- 设置页：SettingsActivity-->
        <activity android:name=".feature.settings.SettingsActivity"
            android:exported="true" />

        <!-- 个人信息页：PersonalInfoActivity-->
        <activity android:name=".feature.settings.PersonalInfoActivity"/>

        <!-- 修改密码页：ChangePwdActivity-->
        <activity android:name=".feature.settings.ChangePwdActivity"/>
    </application>

</manifest>